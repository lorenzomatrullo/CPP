#include <iostream>

// format specifiers % = defines and formats a type of data to be displayed


int main(){

    /*
        %c = character
        %s = strings (array of characters)
        %f = float
        %lf = double (long float)
        %d = integer

        %.1 = decimal precision (1 digit)
        %.2 = decimal precision (2 digits)

        %1 = minimum field width
        %- = left allign
    */

    return 0;
}